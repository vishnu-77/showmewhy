{
  "description": "ShowMeWhy evidence retention and fail-open context compression for verbose Bash tool output",
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "python3 \"${CLAUDE_PLUGIN_ROOT}/hooks/showmewhy-post-tool-use.py\"",
            "timeout": 10
          }
        ]
      }
    ]
  }
}
